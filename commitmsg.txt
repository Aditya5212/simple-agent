fix(mastra): lazy-init storage + mastra proxies; update TODO and WIP notes

Summary:
- Convert Mastra/Postgres initialization to lazy-init and export proxy getters
- Document WIP status, remaining work, and blockers in `todo.md`

Details:
- Implemented lazy-init + proxy exports for Mastra storage to avoid import-time DDL during `next build`. This prevents build-time Postgres DDL races and deadlocks.
- Updated repository TODOs and the WIP commit message to reflect remaining steps: replace dev shims, add E2E smoke tests, and fully validate R2/NVIDIA credentials.

Notes:
- This is a safe WIP message for a feature branch. Don't merge to `main` until the Mastra init and dev shims are resolved and `yarn build` passes cleanly in CI.

Suggested commands to commit this WIP:

    git checkout -b fix/lazy-storage-init
    git add src/mastra/storage.ts src/mastra/index.ts todo.md commitmsg.txt
    git commit -F commitmsg.txt
    git push -u origin fix/lazy-storage-init
